# Monsoon Lily

A small, calm landing page for Monsoon Lily, with an editorial monsoon inspired palette and a direct link to Instagram.

## Run locally

Open `index.html` in a browser, or start a static server from this directory:

```sh
python3 -m http.server 8000
```

Then visit http://localhost:8000.
